####################################################################################################
# UTA (Universal Transcript Archive) configuration
####################################################################################################

export UTA_DB_URL=postgresql://anonymous@localhost:5433/uta/uta_20241220

####################################################################################################
# SeqRepo — local sequence store (optional but recommended for large batch runs)
# Prevents repeated NCBI/Ensembl network fetches during c→g mapping.
# Set to the path of a local seqrepo snapshot; the 'latest' symlink is
# created automatically by `seqrepo pull`.
####################################################################################################

export HGVS_SEQREPO_DIR=/usr/local/share/seqrepo/2024-12-20
